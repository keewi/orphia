Read the following source files and regenerate `docs/game-development-context.md` with the latest state:

- `app/games/showdle/showdle.css` (design system tokens, component styles)
- `lib/db/showdle-schema.ts` (database schema)
- `package.json` (dependencies, tech stack)
- `middleware.ts` (routing, public routes)
- `drizzle.config.ts` (ORM config)
- `app/games/showdle/layout.tsx` (layout pattern)
- `app/games/showdle/page.tsx` (server component pattern)

Keep the same document structure and sections but update all values (colors, fonts, spacing, dependencies, file paths, patterns) to match the current source files. Do not add commentary — just write the updated file.
